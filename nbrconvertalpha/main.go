package main

import (
	"encoding/asn1"
	"github.com/01-edu/z01"
	"os"
)

func main() {
	for _,letter := range os.Args >{
	if os.Args[1] == "--upper" {

	} else if
		if n+96 < 97 || n+96 > 122 {
			z01.PrintRune(32)
		} else {
			n += 96
			z01.PrintRune(rune(n))
		}
	}
}
